#!groovy
pipeline any {
    agent any
    
    stages {
        stage('Setup'){
            steps{
                checkout scm
            }
        }

        stage('docker-build'){
            steps{
            }
        }
    }
    
}
